<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Banner">

    <select id="getAll" resultType="com.ug369.backend.bean.bean.request.BannerRequest">
        SELECT u.id as id,u.content as content,u.title as title,
        u.picture as picture,u.status as status,u.order_id as orderId,
        u.create_date as createDate,u.begin_date as beginDate,u.link as link,
        u.end_date as endDate,
        u.contact_name as contactName,u.contact_phone as contactPhone,
        u.weight as weight,u.isdefault as isdefault
        FROM ug_banner u
    </select>

    <select id="getCount" resultType="int">
        SELECT COUNT(id)
        FROM ug_banner u
    </select>

</mapper>
